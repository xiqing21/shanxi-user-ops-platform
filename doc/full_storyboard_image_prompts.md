# 山西省全量用电用户实时智能运营平台 - 全功能分镜生图文案

> 用途：直接复制到图片生成工具中生成产品原型分镜图。  
> 建议规格：16:9 横屏为主，少量移动端 9:16。  
> 建议数量：30 张。  
> 视觉统一要求：国网绿主色、清新明亮政企系统风格、真实山西地图、shadcn 风格控件、真实业务数据密度、真实按钮和状态，白色和浅灰底色，绿色强调色，不要营销官网，不要暗黑大屏，不要抽象科技背景，不要 3D 大柱子地图。
> 品牌视觉：使用国家电网风格的绿色作为主色，建议色感接近 #007A3D / #00A65A，搭配白色、浅灰、浅绿、少量橙红风险色；整体干净、透亮、专业。

---

## A. 一级页面总览图，12 张

### 01 全省态势首页

```text
生成一张 16:9 产品原型图，主题是“山西省全量用电用户实时智能运营平台 - 全省态势首页”。国网绿主色、清新明亮政企运营系统风格，白色卡片、浅灰页面底色、绿色导航和强调色，真实业务系统质感。顶部导航包含平台名称、角色切换器、省市县面包屑“山西省”、数据源徽标“StarRocks 内表实时”、链路状态“PG -> Flink CDC -> Fluss -> Flink -> StarRocks 正常”。中间是清晰真实的山西省行政区划地图，11 个地市边界明确，按风险热度渐变着色，地图上有大工业用户点位、台区风险点和线路告警点。左侧是组织树和筛选器：用户类型、行业、风险类型、时间范围。右侧是实时告警队列，显示 CEP 告警、离线校准差异、工单状态。底部是今日负荷、昨日负荷、预测负荷、离线校准四条曲线。右下角有悬浮按钮“模拟控制台”。画面要像真实可操作系统，不要宣传海报。
```

### 02 省市县地图下钻页

```text
生成一张 16:9 产品原型图，主题是“山西省 GIS 省市县下钻”。顶部面包屑显示“山西省 / 太原市 / 小店区”，中间地图展示太原市区县边界，小店区高亮，地图上分布台区、线路、大工业企业、告警点位。点位用红橙黄蓝区分过载、重载、低功率因数、正常。左侧显示太原市下属区县列表和风险排名。右侧详情面板展示所选区县：用户数、当前负荷、大工业用户数、风险事件数、Flink 延迟、StarRocks 最近写入时间。底部有区县负荷趋势和台区风险 Top10。整体清新明亮、国网绿主色、真实山西地图、边界清楚，不要柱状地图。
```

### 03 大工业用户分析页

```text
生成一张 16:9 产品原型图，主题是“山西大工业用户专项分析”。顶部 KPI 显示大工业用户数、当前负荷、今日峰值、合同容量风险、功率因数异常、晚高峰贡献率。左侧行业筛选包括煤炭、焦化、钢铁、电解铝、化工、装备制造、园区。中间上方是行业负荷堆叠面积图，中间下方是企业负荷曲线对比。右侧是企业风险排行表，字段包括企业名称、地市、行业、当前负荷、最大需量、功率因数、风险标签。底部是同园区、同线路、同行业联动分析。画面要有真实电力经营分析系统质感。
```

### 04 用户画像详情页

```text
生成一张 16:9 产品原型图，主题是“全量用户画像详情”。页面展示一个大工业用户详情：左侧为用户档案卡，包含用户编号、企业名称、地市区县、行业、合同容量、运行容量、供电线路、台区、计量点。中间为负荷曲线、电量曲线、功率因数曲线、峰谷用电特征。右侧为风险标签：负荷突增、最大需量接近合同容量、低功率因数、疑似复产。底部展示数据血缘：PG 源表、Flink CDC、Fluss DWD、StarRocks ADS。整体像真实业务详情页，信息密度高但清晰。
```

### 05 实时风险与 CEP 控制台

```text
生成一张 16:9 产品原型图，主题是“Flink CEP 实时风险控制台”。左侧是 CEP 规则卡片：重载转过载、三相不平衡持续扩大、大工业突增突降、线路温升。中间是事件匹配时间线，展示某台区从“合理 -> 重载 -> 过载”的连续事件，每个节点有事件时间、负荷率、来源表、窗口编号。右侧是实时告警流，红橙黄分级，展示区域、对象、置信度、处置状态。底部是技术链路小 DAG：PostgreSQL -> Flink CDC -> Fluss -> Flink CEP -> StarRocks -> Web。突出真实链式事件，不要只做普通表格。
```

### 06 Text-to-SQL 问数页

```text
生成一张 16:9 产品原型图，主题是“AI Text-to-SQL 问数”。左侧是聊天输入区，用户问题为“查询太原大工业用户负荷 Top10 并按行业出图表”。中间上方显示 DeepSeek 生成的 StarRocks SQL，旁边有 SQL AST 安全校验通过、只读 SELECT、LIMIT 1000 徽标。中间下方同时展示结果表格和柱状图，图表标题为“太原大工业负荷 Top10”。右侧是 Milvus + BGE-M3 召回证据列表，包含命中的 ADS 表、字段说明、指标口径、历史 SQL。底部按钮：执行查询、生成图表、保存分析、转为 AI 建任务。整体像真实数据分析工具。
```

### 07 AI 从 0 建任务页

```text
生成一张 16:9 产品原型图，主题是“AI 从 0 创建实时数据任务”。顶部步骤条：语义拆解、元数据召回、口径确认、生成任务、样例试跑、性能优化、发布资产、注册向量样本。左侧是用户自然语言需求和多轮口径确认，例如“晚高峰按 18:00-22:00，突增按近 7 日均值 2 倍”。中间是 Agent 生成的任务计划，包含 Flink SQL、Window TVF、StarRocks ADS 表、Paimon 离线快照。右侧是校验结果：样例行数、空值率、延迟、主键建议、分区建议、并行度建议。底部按钮：提交 Flink 作业、发布 StarRocks 表、加入 Milvus 语义层。
```

### 08 数据链路运行拓扑页

```text
生成一张 16:9 产品原型图，主题是“全链路运行拓扑监控”。画面中央是横向 DAG：PostgreSQL 源库 -> Flink CDC -> Fluss ODS/DWD -> Flink SQL/TVF/CEP -> StarRocks 内表 ADS -> Paimon 离线快照 -> StarRocks 外表统一视图 -> API -> Web。每个节点是可点击卡片，显示状态 RUNNING、端口、吞吐、延迟、错误数。下方有 Flink 作业表：pg-to-fluss-cdc、user-load-window-tvf、grid-risk-cep、paimon-hourly-snapshot。右侧有日志摘要和常用命令。画面要让人一眼看懂哪个是 Flink、哪个是 Fluss、哪个是 StarRocks、哪个是 Paimon。
```

### 09 数据模拟与压测中心

```text
生成一张 16:9 产品原型图，主题是“数据模拟与压测中心”。左侧是场景模板卡片：实时遥测流、大批量离线插入、Window 曲线、CEP 链式告警、CDC 更新删除、升级中双写、晚到补采。中间是参数配置面板，可设置用户规模、写入频率、异常比例、持续时间、目标源表 PostgreSQL。右侧显示压测实时指标：PG 写入行数、Flink CDC 延迟、Fluss changelog 条数、StarRocks 写入行数、查询耗时。底部是压测日志和结果报告按钮。强调模拟数据写入 PG 后经完整链路流转。
```

### 10 离线 Batch 与 Paimon 校准页

```text
生成一张 16:9 产品原型图，主题是“流批一体离线校准”。左侧展示 Paimon 快照列表，包含快照版本、分区日期、行数、生成时间。中间展示 Flink Batch 任务进度，任务名 paimon-hourly-snapshot 和 ads-offline-reconcile。右侧展示实时 ADS 与离线 ADS 差异表，字段包括区域、指标、实时值、离线值、差异率、是否修正。顶部有按钮“触发离线 Batch”“切换离线兜底接口”。底部是冷热统一查询示意：StarRocks 内表 + Paimon 外表 UNION ALL。强调离线兜底真实存在。
```

### 11 角色切换与多视角工作台

```text
生成一张 16:9 产品原型图，主题是“角色切换与多视角工作台”。顶部打开角色切换下拉菜单，包含省公司调度运营、市公司运维、县公司班组、数据分析师、数据工程师、演示测试。背景分成多个区域预览，不同角色看到不同重点：省公司看全省地图，市公司看区县，县公司看台区工单，数据工程师看链路 DAG，数据分析师看 Text-to-SQL。界面不需要登录页，只是角色切换影响页面内容和权限按钮。
```

### 12 系统健康与 AI 智能运维页

```text
生成一张 16:9 产品原型图，主题是“AI 智能运维与系统健康”。左侧是服务健康列表：PostgreSQL、Flink JobManager、Fluss Coordinator、StarRocks FE、Paimon Warehouse、Milvus、AI Service、API、Web。中间是异常诊断面板，AI 给出问题定位：Flink CDC 延迟升高、StarRocks 写入积压、Milvus 向量 collection 未加载。右侧是建议动作：重启任务、提高并行度、触发离线兜底、查看日志。底部有指标曲线：延迟、吞吐、错误数、CPU、内存。风格专业，像真实运维系统。
```

---

## B. 关键交互细节图，12 张

### 13 浮动模拟控制台展开

```text
生成一张 16:9 产品细节图，主题是“右下角浮动模拟控制台展开”。背景是全省态势首页，右下角展开一个白色悬浮控制面板，国网绿标题栏，浅绿色状态标签，标题“链路模拟控制台”。面板按钮包括：开启实时数据、停止实时数据、大批量离线插入、注入 Window 数据、注入 CEP 数据、注入 CDC 更新删除、触发离线 Batch、链路体检、清空演示数据。每个按钮有状态灯和简短说明。面板中部显示实时写入速率、PG 行数、Flink CDC 延迟、Fluss changelog 条数、StarRocks 写入行数、Paimon 快照版本。底部有进度条和日志摘要。
```

### 14 开启实时数据后的首页变化

```text
生成一张 16:9 产品细节图，主题是“开启实时数据后的首页动态变化”。首页 KPI 数字正在增长，总用户数、当前负荷、告警数、StarRocks 写入行数均有绿色跳动效果。地图上多个地市热力颜色发生变化，右侧实时告警队列不断新增。右下角模拟控制台显示“实时中”，写入速率 1200 rows/s，目标 PostgreSQL ods_meter_reading，链路状态全部绿色。画面要体现实时链路真的在跑。
```

### 15 大批量离线插入进度

```text
生成一张 16:9 产品细节图，主题是“大批量离线插入进度”。浮动控制台处于压测模式，参数为“插入 1000000 条历史遥测数据”，进度条显示 46%，PG 批量写入速率、Flink CDC 消费延迟、StarRocks 写入吞吐、Paimon 分区生成数量都在变化。背景是链路拓扑页，PostgreSQL、Flink CDC、Fluss、StarRocks 节点高亮。画面要像真实压测现场。
```

### 16 Window TVF 数据注入细节

```text
生成一张 16:9 产品细节图，主题是“Window TVF 曲线数据注入”。左侧控制台选择场景“峰谷曲线 + 突增突降”，配置窗口为 1 分钟 TUMBLE、5 分钟 HOP、日累计 CUMULATE。中间显示三条窗口结果曲线：实时负荷、窗口峰值、累计电量。右侧显示 Flink SQL Window TVF 任务状态 RUNNING 和输出表 dws_area_window_load。强调窗口函数的产品可见效果。
```

### 17 CEP 数据注入细节

```text
生成一张 16:9 产品细节图，主题是“CEP 链式告警注入”。控制台选择“重载转过载”场景，生成事件序列：合理 62%、重载 88%、过载 107%。中间时间线展示事件一步步匹配，右侧告警卡片从待观察变成高风险告警。底部显示 Flink CEP MATCH_RECOGNIZE 作业输出到 StarRocks ads_risk_event。画面突出链式事件，不是普通阈值告警。
```

### 18 CDC 更新删除与撤回细节

```text
生成一张 16:9 产品细节图，主题是“CDC 更新删除与告警撤回”。左侧显示 PostgreSQL 源表发生 UPDATE 和 DELETE，更新一条错误遥测数据，删除一条无效计量点。中间链路显示 Flink CDC 捕获 changelog，Fluss 保存 UPDATE_BEFORE、UPDATE_AFTER、DELETE。右侧 StarRocks 主键表修正指标，风险队列中一条过载告警被标记“已撤回”。底部有说明“CEP 链路只接收 append-only 桥接事件，离线校准负责纠偏”。画面要体现 retraction 真实处理。
```

### 19 Text-to-SQL 生成图表按钮细节

```text
生成一张 16:9 产品细节图，主题是“Text-to-SQL 结果生成图表”。页面聚焦在查询结果区域，顶部展示 SQL 已执行成功，数据源“StarRocks 内表”。结果表格旁边有图表类型选择按钮：柱状图、折线图、面积图、饼图、导出。当前选择柱状图，生成太原大工业负荷 Top10 柱状图。右侧证据面板展示 Milvus 命中的字段和指标。画面要让人看到“输入后能出数，也能出图表”。
```

### 20 AI 建任务多轮口径确认

```text
生成一张 16:9 产品细节图，主题是“AI 建任务多轮口径确认”。左侧对话中 AI 提问：“突增口径按昨日同小时超过 30%，还是近 7 日均值 2 倍？”用户选择“近 7 日均值 2 倍”。中间任务计划自动更新，新增 DWS 表 dws_user_load_baseline 和 ADS 表 ads_industry_peak_anomaly。右侧显示影响评估：需要 Flink HOP 窗口、StarRocks 主键模型、Paimon 离线校准。画面强调多轮交互和从 0 建宽表。
```

### 21 Flink 作业提交成功细节

```text
生成一张 16:9 产品细节图，主题是“Flink 作业正式提交成功”。页面是数据工程师视角，弹窗显示作业提交结果：pg-to-fluss-cdc RUNNING、user-load-window-tvf RUNNING、grid-risk-cep RUNNING。背景有 Flink 作业列表，包含 Job ID、状态、并行度、checkpoint、backpressure、输入输出速率。右侧显示提交命令摘要和最近日志。画面要真实，不要只写“提交成功”四个字。
```

### 22 StarRocks 内表查询细节

```text
生成一张 16:9 产品细节图，主题是“StarRocks 内表实时 ADS 查询”。中间是 SQL 查询面板，查询 ads_realtime_user_load 和 ads_realtime_risk_events，结果显示 500000 用户、实时负荷、告警数量、最近更新时间。左侧是 StarRocks 表列表：主键表、聚合表、统一视图。右侧显示查询耗时 87ms、扫描行数、命中分区。顶部徽标写着“前端数据来自 StarRocks，不是前端 mock”。画面强调真实查询服务层。
```

### 23 Milvus + LanceDB 资料治理细节

```text
生成一张 16:9 产品细节图，主题是“Milvus + LanceDB AI 资料治理”。左侧显示原始资料：Excel 功能点、业务文档、DDL、指标口径、历史 SQL。中间显示 LanceDB 管理 chunk、标签、版本、embedding 模型、评测样本。右侧显示 Milvus 在线 collection，向量条数、BGE-M3 模型、召回 TopK。底部展示 Text-to-SQL 使用这些证据生成 SQL。画面要解释 LanceDB 是资料湖和版本管理，Milvus 是在线向量召回。
```

### 24 告警工单处置抽屉

```text
生成一张 16:9 产品细节图，主题是“告警工单处置抽屉”。背景是地图首页，用户点击一条过载告警后右侧弹出详情抽屉。抽屉展示告警对象、地市区县、台区、用户、负荷率、持续时间、CEP 匹配路径、相关曲线、影响用户数、建议动作。底部有按钮：确认告警、派发工单、标记误报、查看离线校准、查看源数据。画面体现业务闭环，不只是展示告警。
```

---

## C. 技术链路与异常兜底细节图，6 张

### 25 完整数据链路架构图

```text
生成一张 16:9 技术产品混合分镜图，主题是“PG 到 StarRocks 的实时湖仓完整链路”。用清晰横向架构图表达：PostgreSQL 源库 -> Flink CDC YAML -> Fluss ODS Changelog -> Flink SQL 清洗 -> Fluss DWD/DWS -> Flink Window TVF 和 CEP -> StarRocks 内表 ADS -> API -> Web。另一路从 Fluss 自动分层到 Paimon，StarRocks 外表读取 Paimon，形成冷热统一视图。每个节点都有端口、状态、数据量、延迟。风格是产品里的运行拓扑，不是 PPT 简图。
```

### 26 实时失败时离线兜底

```text
生成一张 16:9 产品细节图，主题是“实时链路异常时离线兜底”。画面顶部出现黄色告警：Flink CEP 延迟升高，实时指标可能不稳定。中间链路图中 Flink 节点黄色闪烁，右侧系统自动切换部分查询到 StarRocks 外表 + Paimon 离线快照。下方差异表显示实时值、离线值、差异率、修正状态。按钮包括“触发离线 Batch”“恢复实时接口”“查看异常日志”。强调系统健壮性和兜底。
```

### 27 升级中双写对账场景

```text
生成一张 16:9 产品细节图，主题是“升级中双写对账场景”。左侧显示旧任务 old-user-load-window 继续运行，中间显示新任务 new-user-load-window 灰度运行，二者同时写入不同 ADS 表。右侧对账面板比较两个版本的用户数、总负荷、告警数、Top10 排名差异。底部显示 schema 变更记录和回滚按钮。画面体现升级中场景和测试验证能力。
```

### 28 Docker Compose 一键启动完成页

```text
生成一张 16:9 产品/终端混合图，主题是“一键启动全栈完成”。左侧是终端输出，命令为 ./scripts/start-full-stack.sh，输出服务地址：Flink UI、StarRocks FE、PostgreSQL、Fluss、Web、API、AI、Milvus、MinIO。右侧是 Web 运行拓扑页面，所有节点状态 RUNNING。底部显示 Running jobs：pg-to-fluss-cdc、user-load-window-tvf、grid-risk-cep、paimon-hourly-snapshot。画面强调一个脚本启动，不需要用户看大量 Docker 日志。
```

### 29 县公司移动值班视图

```text
生成一张 9:16 移动端产品原型图，主题是“县公司值班告警视图”。清新明亮移动端界面，国网绿顶部栏，白色卡片，顶部显示当前角色“县公司班组”和区域“小店区”。第一屏 KPI 包括异常用户、过载台区、待处理工单、链路状态。下方是告警卡片流，每张卡片包含台区、用户、风险类型、负荷率、发生时间、处置按钮。底部有简化地图缩略图和按钮：上报处置、查看链路、联系抢修。不要营销风，像真实值班工具。
```

### 30 全功能演示收束图

```text
生成一张 16:9 总结型产品分镜图，主题是“山西省全量用电用户实时智能运营平台完整闭环”。画面分成四象限：左上是山西地图实时态势，右上是 Text-to-SQL 出表出图，左下是浮动模拟控制台和 CEP 告警，右下是 PG -> Flink CDC -> Fluss -> Flink -> StarRocks/Paimon 链路拓扑。中间用一句话标题：“从源库造数，到实时计算，到智能问数，到离线兜底”。整体要像产品演示封面，但仍保留真实控件、表格、状态和业务数据。
```

---

## 统一负向提示词

```text
不要登录页，不要营销官网，不要纯概念科技背景，不要暗黑风格，不要一根巨大 3D 柱子地图，不要抽象山西轮廓，不要只有蓝色发光线条，不要暗色霓虹，不要把 Flink、Fluss、StarRocks、Paimon 混成一个黑盒，不要出现煤改电单一主题，不要卡通风，不要低信息密度，不要只有假数字没有按钮和表格，不要英文为主，界面文案以中文为主。
```

