# 山西省全量用电用户实时智能运营平台 - 简化版分镜生图文案

> 用途：直接复制到图片生成工具中，生成产品原型分镜图。  
> 建议数量：12 张。  
> 建议规格：主图 16:9 横屏，移动值班图 9:16。  
> 视觉统一：国网绿主色，清新明亮政企系统风格，白色卡片，浅灰页面底色，浅绿色状态标签，少量橙红表达风险。不要暗黑大屏，不要蓝色霓虹，不要 3D 大柱子地图，不要营销官网。

---

## 01 全省运营总览

```text
生成一张 16:9 产品原型图，主题是“山西省全量用电用户实时智能运营平台 - 全省运营总览”。整体使用国网绿主色，清新明亮政企系统风格，白色卡片和浅灰背景。顶部包含平台名称、角色切换、省市县面包屑“山西省”、数据源徽标“StarRocks 内表实时”、链路状态“PG -> Flink CDC -> Fluss -> Flink -> StarRocks 正常”。中间是清晰真实的山西省行政区划地图，显示 11 个地市边界，按风险热度着色，地图上有重点大工业用户点位、线路风险点、台区告警点。左侧是组织树和筛选器：用户类型、行业、风险类型、时间范围。右侧是实时告警队列，包含 CEP 告警、离线校准差异、工单状态。底部是今日负荷、昨日负荷、预测负荷、离线校准四条曲线。右下角有“模拟控制台”悬浮按钮。画面要像真实可用的电力运营系统。
```

## 02 省市县地图下钻

```text
生成一张 16:9 产品原型图，主题是“山西省 GIS 省市县下钻”。使用国网绿和浅色背景。顶部面包屑显示“山西省 / 太原市 / 小店区”。中间地图展示太原市区县边界，小店区高亮，地图上有线路、台区、大工业企业和告警点位，颜色区分正常、重载、过载、低功率因数。左侧是太原市下属区县风险排名。右侧是所选区县详情：用户数、当前负荷、大工业用户、风险事件数、Flink 延迟、StarRocks 最近写入时间。底部展示区县负荷趋势和台区风险 Top10。地图必须是正常山西行政区划风格，不要抽象图。
```

## 03 大工业用户分析

```text
生成一张 16:9 产品原型图，主题是“山西大工业用户专项分析”。国网绿主色，清新明亮。顶部 KPI 展示大工业用户数、当前负荷、今日峰值、合同容量风险、功率因数异常、晚高峰贡献率。左侧行业筛选：煤炭、焦化、钢铁、电解铝、化工、装备制造、园区。中间是行业负荷堆叠面积图和企业负荷曲线对比。右侧是企业风险排行表，字段包含企业名称、地市、行业、当前负荷、最大需量、功率因数、风险标签。底部展示同园区、同线路、同行业联动分析。画面要有真实国网经营分析系统质感。
```

## 04 用户画像与业务详情

```text
生成一张 16:9 产品原型图，主题是“全量用户画像与用电详情”。展示一个大工业用户详情页。左侧为用户档案卡：用户编号、企业名称、地市区县、行业、合同容量、运行容量、供电线路、台区、计量点。中间为负荷曲线、电量曲线、功率因数曲线、峰谷用电特征。右侧为风险标签和处置建议：负荷突增、最大需量接近合同容量、低功率因数、疑似复产。底部展示数据来源血缘：PostgreSQL 源表 -> Flink CDC -> Fluss DWD -> StarRocks ADS。视觉清爽、专业、信息清楚。
```

## 05 实时风险与 CEP 告警

```text
生成一张 16:9 产品原型图，主题是“实时风险与 Flink CEP 告警”。国网绿浅色系统风格，风险使用橙红点缀。左侧是 CEP 规则卡片：重载转过载、三相不平衡、大工业突增突降、线路温升。中间是链式事件时间线，展示某台区从“合理 -> 重载 -> 过载”的连续过程，每个节点有事件时间、负荷率、来源表。右侧是告警队列和工单处置抽屉，包含确认告警、派发工单、标记误报、查看离线校准按钮。底部有小型链路图：PG -> Flink CDC -> Fluss -> Flink CEP -> StarRocks -> Web。画面要说明 CEP 的业务价值，不要只画普通列表。
```

## 06 AI Text-to-SQL 问数

```text
生成一张 16:9 产品原型图，主题是“AI Text-to-SQL 智能问数”。清新明亮，国网绿强调色。左侧是自然语言问数对话，用户输入“查询太原大工业用户负荷 Top10 并按行业出图表”。中间上方显示 DeepSeek 生成的 StarRocks SQL，旁边有 SQL AST 安全校验通过、只读 SELECT、LIMIT 1000 徽标。中间下方展示查询结果表格和柱状图，数据源标记“StarRocks 内表实时”。右侧是 Milvus + BGE-M3 召回证据，包含命中的表、字段、指标口径和历史 SQL。底部按钮：执行查询、生成图表、保存分析、转为 AI 建任务。
```

## 07 AI 从 0 创建数据任务

```text
生成一张 16:9 产品原型图，主题是“AI 从 0 创建实时数据任务”。顶部步骤条包含：语义拆解、元数据召回、口径确认、生成任务、样例试跑、优化发布。左侧是多轮口径确认，例如“晚高峰按 18:00-22:00，突增按近 7 日均值 2 倍”。中间是 Agent 生成的任务计划，包含 Flink SQL、Window TVF、StarRocks ADS 表、Paimon 离线快照。右侧是试跑和优化结果：样例行数、空值率、延迟、主键建议、分区建议、并行度建议。底部有提交 Flink 作业、发布 StarRocks 表、加入 Milvus 语义层按钮。不要展示冗长思维链，只展示可审核的任务计划。
```

## 08 模拟控制与压测入口

```text
生成一张 16:9 产品原型图，主题是“模拟控制与压测入口”。背景是全省运营总览，右侧展开一个白色悬浮控制面板，国网绿标题栏，标题为“链路模拟控制台”。面板里集中展示主要能力：开启实时数据、停止实时数据、大批量离线插入、注入 Window 数据、注入 CEP 数据、注入 CDC 更新删除、触发离线 Batch、链路体检。每个按钮旁有状态灯和简短说明。面板下方显示当前写入速率、PG 行数、Flink CDC 延迟、Fluss changelog 条数、StarRocks 写入行数、Paimon 快照版本。不要单独强调进度细节，只要让人看懂这里能控制整条演示链路。
```

## 09 全链路运行拓扑

```text
生成一张 16:9 产品原型图，主题是“全链路运行拓扑监控”。清新明亮国网绿风格。画面中央是横向 DAG：PostgreSQL 源库 -> Flink CDC -> Fluss ODS/DWD -> Flink SQL/TVF/CEP -> StarRocks 内表 ADS -> Paimon 离线快照 -> StarRocks 外表统一视图 -> API -> Web。每个节点是可点击卡片，显示 RUNNING 状态、端口、吞吐、延迟、错误数。下方是 Flink 作业列表：pg-to-fluss-cdc、user-load-window-tvf、grid-risk-cep、paimon-hourly-snapshot。右侧有日志摘要和常用命令。重点是让人一眼看懂哪个是 Flink、哪个是 Fluss、哪个是 StarRocks、哪个是 Paimon。
```

## 10 流批一体与离线兜底

```text
生成一张 16:9 产品原型图，主题是“流批一体与离线兜底”。左侧展示 Paimon 快照列表，包含快照版本、分区日期、行数、生成时间。中间展示 Flink Batch 任务和离线校准流程。右侧展示实时 ADS 与离线 ADS 差异表，字段包括区域、指标、实时值、离线值、差异率、是否修正。顶部有按钮“触发离线 Batch”“切换离线兜底接口”。底部展示冷热统一查询：StarRocks 内表 + Paimon 外表 UNION ALL。画面要说明为什么实时出问题时还有离线兜底。
```

## 11 AI 智能运维与系统健康

```text
生成一张 16:9 产品原型图，主题是“AI 智能运维与系统健康”。左侧是服务健康列表：PostgreSQL、Flink JobManager、Fluss Coordinator、StarRocks FE、Paimon Warehouse、Milvus、AI Service、API、Web。中间是异常诊断面板，AI 给出问题定位：Flink CDC 延迟升高、StarRocks 写入积压、Milvus collection 未加载。右侧是建议动作：查看日志、提高并行度、触发离线兜底、重启任务。底部有延迟、吞吐、错误数、CPU、内存曲线。整体清新、专业、像真实运维平台。
```

## 12 完整闭环总结图

```text
生成一张 16:9 总结型产品分镜图，主题是“山西省全量用电用户实时智能运营平台完整闭环”。国网绿主色、明亮政企风格。画面分成四个区域：左上是山西地图实时态势，右上是 Text-to-SQL 出表出图，左下是模拟控制台和 CEP 告警，右下是 PG -> Flink CDC -> Fluss -> Flink -> StarRocks/Paimon 链路拓扑。中间标题：“从源库造数，到实时计算，到智能问数，到离线兜底”。画面要适合作为产品方案封面，同时保留真实控件、表格、状态和业务数据。
```

---

## 可选移动端补充图

如果需要移动端，再额外生成这一张；如果只做桌面原型，可以不生成。

```text
生成一张 9:16 移动端产品原型图，主题是“县公司值班告警视图”。清新明亮移动端界面，国网绿顶部栏，白色卡片。顶部显示当前角色“县公司班组”和区域“小店区”。第一屏 KPI 包括异常用户、过载台区、待处理工单、链路状态。下方是告警卡片流，每张卡片包含台区、用户、风险类型、负荷率、发生时间、处置按钮。底部有简化地图缩略图和按钮：上报处置、查看链路、联系抢修。像真实值班工具，不要营销风。
```

---

## 统一负向提示词

```text
不要登录页，不要营销官网，不要暗黑大屏，不要蓝色霓虹，不要一根巨大 3D 柱子地图，不要抽象山西轮廓，不要把 Flink、Fluss、StarRocks、Paimon 混成一个黑盒，不要出现煤改电单一主题，不要卡通风，不要低信息密度，不要只有假数字没有按钮和表格，不要英文为主，界面文案以中文为主。
```

